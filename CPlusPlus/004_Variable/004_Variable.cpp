
int main()
{
    int PlayerAtt;
    // 위 코드는 변수를 생성한다는 코드이다.
    // RAM으로 올라가면 해당 코드는 아래와 같은 명령 프로세스로 변한다.

    // "int(정수형) 만큼의 크기인 'PlayerAtt' 라는 이름의 변수를 생성해라."

    // 위 프로세스는 CPU가 받아서 처리하게 된다.
    // CPU가 프로세스를 처리하면 RAM에 해당 자료형 만큼의 크기의 메모리공간에 값을 적재하고 'PlayerAtt'라는 이름으로 지정한다.

    
    10;
    // 위 코드는 10이라고만 적혀서 문제가 있는 코드로 보인다.
    // 하지만 위 코드도 메모리를 사용하여 10을 저장한다.
    // 이렇게 되면 10이 어디있는지 모르게 되고 찾을 수도 없다.
    // 하여 자료형과 변수명을 아래와 같이 명확히 나타내주어야 한다.
    int val = 10;

}

